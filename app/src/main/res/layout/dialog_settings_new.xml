<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/wm_card">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="设置"
        android:textSize="20sp"
        android:textColor="@color/wm_text"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="20dp" />

    <!-- Range Display -->
    <TextView
        android:id="@+id/tvRangeDisplay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="当前范围: 0% - 10%"
        android:textSize="14sp"
        android:textColor="@color/wm_text_secondary"
        android:padding="12dp"
        android:background="@drawable/form_tag_background"
        android:layout_marginBottom="20dp" />

    <!-- Dictionary Management -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="词典管理"
        android:textSize="16sp"
        android:textColor="@color/wm_text"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btnImportDict"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📚 导入词典"
        android:textSize="14sp"
        android:layout_marginBottom="24dp"
        style="@style/Widget.Material3.Button.OutlinedButton" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/btnCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="取消"
            android:textSize="14sp"
            android:layout_marginEnd="8dp"
            style="@style/Widget.Material3.Button.TextButton" />

        <Button
            android:id="@+id/btnApply"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="应用"
            android:textSize="14sp"
            style="@style/Widget.Material3.Button" />
    </LinearLayout>

</LinearLayout>
